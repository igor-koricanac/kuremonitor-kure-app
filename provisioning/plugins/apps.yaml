apiVersion: 1

apps:
  - type: 'kuremonitor-kure-app'
    org_id: 1
    org_name: 'kure-monitor'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
